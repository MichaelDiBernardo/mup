{
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.56.0",
    "parcel": "^2.11.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "start": "parcel src/index.html",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .parcel-cache",
    "dist": "yarn clean && parcel build src/index.html --public-url https://mikedebo.com/mup2"
  },
  "dependencies": {}
}
